import l from "libs/langs/keys"

export default {
  date: {
    formats: {
      // [l.sysMsgTimestamp]: "%Y-%m-%d %H:%M:%S",
    }
  },
  [l.errorMessage]: "Oops, it seems that there is an error!"
}
